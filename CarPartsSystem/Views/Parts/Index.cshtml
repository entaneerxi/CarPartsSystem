@model IEnumerable<CarPartsSystem.Models.Part>
@{
    ViewData["Title"] = "Car Parts";
}

<div class="container">
    <div class="row mb-4">
        <div class="col-md-12">
            <h1 class="display-4"><i class="fas fa-cogs"></i> Car Parts</h1>
            <p class="lead">Browse our selection of high-quality car parts</p>
        </div>
    </div>

    <!-- Filters and Search -->
    <div class="row mb-4">
        <div class="col-md-12">
            <form method="get" class="row g-3">
                <div class="col-md-4">
                    <input type="text" name="search" class="form-control" placeholder="Search parts..." value="@ViewBag.SearchTerm" />
                </div>
                <div class="col-md-3">
                    <select name="category" class="form-select">
                        <option value="">All Categories</option>
                        @foreach (var cat in ViewBag.Categories as List<string>)
                        {
                            <option value="@cat" selected="@(ViewBag.SelectedCategory == cat)">@cat</option>
                        }
                    </select>
                </div>
                <div class="col-md-3">
                    <select name="sortBy" class="form-select">
                        <option value="">Sort By</option>
                        <option value="name" selected="@(ViewBag.SortBy == "name")">Name</option>
                        <option value="price_asc" selected="@(ViewBag.SortBy == "price_asc")">Price: Low to High</option>
                        <option value="price_desc" selected="@(ViewBag.SortBy == "price_desc")">Price: High to Low</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn btn-primary w-100"><i class="fas fa-search"></i> Search</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Parts Grid -->
    <div class="row">
        @if (Model.Any())
        {
            @foreach (var part in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card part-card h-100">
                        @if (!string.IsNullOrEmpty(part.ImagePath))
                        {
                            <img src="@part.ImagePath" class="card-img-top" alt="@part.PartName" />
                        }
                        else
                        {
                            <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center" style="height: 200px;">
                                <i class="fas fa-cog fa-5x text-white-50"></i>
                            </div>
                        }
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@part.PartName</h5>
                            <p class="card-text text-muted small">@(part.Brand ?? "Generic")</p>
                            <p class="card-text">@(part.Description?.Substring(0, Math.Min(part.Description.Length, 100)) + (part.Description?.Length > 100 ? "..." : ""))</p>
                            <div class="mt-auto">
                                <div class="d-flex justify-content-between align-items-center mb-2">
                                    <span class="badge bg-info">@part.Category</span>
                                    <span class="text-muted small">Stock: @part.StockQuantity</span>
                                </div>
                                <div class="d-flex justify-content-between align-items-center">
                                    <h4 class="text-primary mb-0">฿@part.Price.ToString("N2")</h4>
                                    <div>
                                        <a asp-action="Details" asp-route-id="@part.PartId" class="btn btn-sm btn-outline-primary">
                                            <i class="fas fa-info-circle"></i> Details
                                        </a>
                                        @if (User.Identity?.IsAuthenticated == true)
                                        {
                                            <button type="button" class="btn btn-sm btn-primary add-to-cart" data-part-id="@part.PartId">
                                                <i class="fas fa-cart-plus"></i>
                                            </button>
                                        }
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-md-12">
                <div class="alert alert-info text-center">
                    <i class="fas fa-info-circle fa-3x mb-3"></i>
                    <h4>No parts found</h4>
                    <p>Try adjusting your search or filter criteria.</p>
                </div>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('.add-to-cart').click(function() {
                var partId = $(this).data('part-id');
                var button = $(this);
                
                button.prop('disabled', true);
                
                $.ajax({
                    url: '/Cart/AddToCart',
                    type: 'POST',
                    data: { partId: partId, quantity: 1 },
                    success: function(response) {
                        Swal.fire({
                            icon: 'success',
                            title: 'Added to Cart!',
                            text: 'Item has been added to your cart.',
                            timer: 2000,
                            showConfirmButton: false
                        });
                        button.prop('disabled', false);
                    },
                    error: function() {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: 'Failed to add item to cart.',
                        });
                        button.prop('disabled', false);
                    }
                });
            });
        });
    </script>
}
